This repo is meant to deploy the ai chatbot workflow automation tool DIFY to a serverless AWS setup for the organization "ForFounders"

A similar repo with custom changes allowing the user to deploy custom images instead of official dify docker hub images can be found here:

/home/acb/WSL2-Client-Work/CreativePlanning/GBA/GBA-DIFY

Likewise, another example that does NOT use a custom domain, which is the case for this project, can be found here:

/home/acb/WSL2-Client-Work/CreativePlanning/DeBruce/DeBruce-DIFY

But DeBruce-DIFY version of this codebase lacks the ability at the moment to use a custom built image from ECR.  But, otherwise is a known working good setup for DIFY version 1.9.1 using the official dockerhub iamge.

